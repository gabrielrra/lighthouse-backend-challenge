- A product can have more than one sale
- If a product has more than one sale, only the one with the highest discount will apply
- A sale can only be applied to a single product and can only give a discount on another one product or itself

On start:

1. `npm run db:migrate`
2. `npm run db:seed`
